<UserControl x:Class="WpfPlayground.View.DataTempView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfPlayground.ViewModel"
             xmlns:model="clr-namespace:WpfPlayground.Model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:DataTempViewModel/>
    </UserControl.DataContext>
   <!-- <UserControl.Resources>
        <DataTemplate DataType="{x:Type model:Person}">
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock Text="이름:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Name}"/>
                <TextBlock Text="나이:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Age}"/>
                <TextBlock Text="NickName:" FontWeight="Bold"/>
                <TextBlock Text="{Binding NickName}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>-->

    <Grid>
        <ListBox ItemsSource="{Binding People}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="이름:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="나이:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Age}"/>
                        <TextBlock Text="NickName:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding NickName}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
