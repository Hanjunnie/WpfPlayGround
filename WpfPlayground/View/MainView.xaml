<UserControl x:Class="WpfPlayground.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dex ="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:local="clr-namespace:WpfPlayground.View"
        xmlns:viewModel ="clr-namespace:WpfPlayground.ViewModel">
    <UserControl.DataContext>
        <viewModel:MainViewMdel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <viewModel:SubMainViewModel x:Key="SubVM" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="HelloWPF" Grid.Row="0" Text="Hello WPF!" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <dex:TextEdit x:Name="devTextBlock" Grid.Row="1" EditValue="{Binding UserName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"  Width="200" FontSize="14"/>
        <dex:TextEdit x:Name="subTextBlock" Grid.Row="2" EditValue="{Binding Source={StaticResource ResourceKey=SubVM}, Path=Address, UpdateSourceTrigger=PropertyChanged}" Width="200" FontSize="14"/>
        <local:VisibilityConvertView Grid.Row="3"/>
        <local:StringMulltiConverterView Grid.Row="4"/>

    </Grid>
        
</UserControl>
