<UserControl x:Class="WpfPlayground.View.StringMulltiConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfPlayground.ViewModel"
             xmlns:utility ="clr-namespace:WpfPlayground.Utility"
             xmlns:dxe ="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:STringMultiConverterViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <utility:StringMultiConverter x:Key="stringMultiConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="23*"/>
            <RowDefinition Height="22*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.RowSpan="2">
            <dxe:TextEdit x:Name="firstName" EditValue="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
            <dxe:TextEdit x:Name="SecondName" EditValue="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock FontWeight ="Bold" FontSize="14">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource stringMultiConverter}">
                        <Binding Path="FirstName"/>
                        <Binding Path="LastName"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
